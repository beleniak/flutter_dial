name: flutter_dial
description: A skeuomorphic dial widget that outputs rotation degrees, percent rotation, and current dial stop.
version: 0.1.4
homepage: https://beleniak.ca
repository: https://github.com/beleniak/flutter_dial.git

environment:
  sdk: ">=3.2.0 <4.0.0"
#  flutter: "3.29.2"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
